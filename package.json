{
  "version": "1.0.0",
  "devDependencies": {
    "tfx-cli": "^0.8.3",
    "typescript": "^4.1.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p .",
    "package": "tfx extension create"
  },
  "dependencies": {
    "vss-web-extension-sdk": "^5.141.0"
  }
}
